1) Pushing changes to staging using
  git add .
2) Check the git status using
  git status 
3) Commit the changes done in the project using
  git commit -m "[commit_message]"
4) Push the commit to the local repository
  git push -v
5) Push the commits to the github (origin master)
  git push -f origin master
